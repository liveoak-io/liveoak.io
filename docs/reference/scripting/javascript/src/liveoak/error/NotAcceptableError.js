
/**
 * A LiveOak error for when a request is not acceptable.
 *
 * This is most likely due to the request missing a parameter or not supplying 
 * a parameter in a proper format. The error message should contain the reason for
 * the failure.
 *
 * @class
 * @memberof liveoak
 * @param {String} [message] - The error message
 */
function NotAcceptableError(message) {

/**
 * The errors name: "liveoak.NotAcceptableError"
 * @type {String}
 */
var name;

/**
 * The errors message
 * @type {String} 
 */
var message

}
