
/**
 * A LiveOak error for when trying to perform an action against a resource which
 * does not currently exist.
 *
 * @class
 * @memberof liveoak
 * @param {String} [message] - The error message
 */
function ResourceNotFoundError(message) {

/**
 * The errors name: "liveoak.ResourceNotFoundError"
 * @type {String}
 */
var name;

/**
 * The errors message
 * @type {String} 
 */
var message

}
