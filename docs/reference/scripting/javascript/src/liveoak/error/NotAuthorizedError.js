
/**
 * A LiveOak error for when trying to access a resource when the current
 * user does not have permission to do so.
 *
 * @class
 * @memberof liveoak
 * @param {String} [message] - The error message
 */
function NotAuthorizedError(message) {

/**
 * The errors name: "liveoak.NotAuthorizedError"
 * @type {String}
 */
var name;

/**
 * The errors message
 * @type {String} 
 */
var message

}
