/**
 * A LiveOak Error for when attempting to read from a resource which does
 * not support reading of its contents.
 *
 * @class
 * @memberof liveoak
 * @param {String} [message] - The error message
 */
function ReadNotSupportedError(message) {

  /**
   * The errors name: "liveoak.ReadNotSupportedError"
   * @type {String}
   */
  var name;

  /**
   * The errors message
   * @type {String}
   */
  var message

}
