/**
 * A LiveOak Error for when attempting to update a resource which does
 * not support updating.
 *
 * @class
 * @memberof liveoak
 * @param {String} [message] - The error message
 */
function UpdateNotSupportedError(message) {

  /**
   * The errors name: "liveoak.UpdateNotSupportedError"
   * @type {String}
   */
  var name;

  /**
   * The errors message
   * @type {String}
   */
  var message

}
